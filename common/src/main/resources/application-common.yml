logging:
  config: classpath:log4j2.xml

com: 
  liyulin: 
    apiVersion: 1.0.0
  
server:
  servlet:
    context-path: /  compression:    enabled: true
  undertow:    direct-buffers: true
# 监控跟踪
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*'