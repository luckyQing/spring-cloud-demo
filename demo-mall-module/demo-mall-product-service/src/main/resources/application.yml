basic:
  data-machineId: 1
spring:
  application:
    name: demo-product-service
  profiles:
    include: 
      - common
      - common-db
      - eureka
      - product-db
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/demo_product?${spring.datasource.url-common-paramters}
    username: root
    password: 123456

server:
  port: 20021